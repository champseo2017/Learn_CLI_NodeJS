#!/usr/bin/env node
/* 
`#!/usr/bin/env node` เป็นบรรทัดพิเศษที่เรียกว่า shebang ซึ่งใส่ไว้ที่จุดเริ่มต้นของไฟล์สคริปต์ เพื่อบอกให้ระบบปฏิบัติการรู้ว่าต้องใช้โปรแกรม Node.js ในการรันไฟล์นั้น

การใช้ `#!/usr/bin/env node` แทนที่จะเป็น `#!/usr/bin/node` นั้นมีข้อดีคือ:
1. ทำให้สคริปต์สามารถทำงานได้โดยไม่ขึ้นกับตำแหน่งที่ติดตั้ง Node.js
2. ใช้ Node.js เวอร์ชันที่ถูกตั้งค่าเป็นค่าเริ่มต้นในระบบ

เมื่อมี shebang นี้อยู่ที่ต้นไฟล์ เราสามารถรันสคริปต์ได้โดยตรง เช่น `./myscript.js` โดยไม่ต้องพิมพ์ `node myscript.js` (แน่นอนว่าไฟล์ต้องมีสิทธิ์ executable ก่อน)

สรุปสั้นๆ คือ shebang ทำให้ไฟล์ .js กลายเป็นสคริปต์ที่รันได้ด้วยตัวมันเอง โดยไม่ต้องเรียก node นำหน้าชื่อไฟล์

*/